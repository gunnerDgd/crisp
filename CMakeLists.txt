cmake_minimum_required(VERSION 3.10)
if    (WIN32)
    add_subdirectory(preset/win32)
elseif(UNIX AND NOT APPLE)
elseif(APPLE)
endif ()

if(PRESET_BUILD_EXAMPLES)
    include_directories(src)
    add_subdirectory   (examples)
endif()